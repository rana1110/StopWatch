<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cnb.stopwatchapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/timer_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/timer_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.StopwatchApp">
        <!--<activity
            android:name=".StopWatchActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.StopwatchApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>-->

        <activity
            android:name=".ChronometerStopWatchActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.StopwatchApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>